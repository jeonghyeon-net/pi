
         ╔════════════════════════════════════════╗
         ║             jeonghyeon/pi              ║
         ║    personal pi-coding-agent config     ║
         ╚════════════════════════════════════════╝

  INSTALL
    npm install -g @mariozechner/pi-coding-agent
    pi install https://github.com/jeonghyeon-net/pi

  UPDATE
    pi update

  EXTENSIONS
    mcp … MCP 서버 통합 (proxy tool, lazy connection, metadata caching)
    subagent … `@tintinweb/pi-subagents` 래퍼, npm install + 단일 번들 노출
    tasks … `@tintinweb/pi-tasks` 래퍼, npm install + 단일 번들 노출

  SKILLS
    (none)

  PROMPTS
    (none)

  THEMES
    (none)

  LINKS
    pi-coding-agent  https://github.com/badlogic/pi-mono
