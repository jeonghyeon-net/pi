
  세션의 최근 대화 맥락을 짧게 요약해서 우상단 오버레이, 세션 제목, 터미널 제목을 함께 유지한다.

  FEATURES
    - editor 위 위젯 대신 화면 우상단의 비포커스(non-capturing) 오버레이 박스로 표시
    - 첫 메시지 1회성이 아니라, agent 작업이 완전히 끝난 시점마다 요약 갱신
    - follow-up 처리 전의 idle 경계(agent_end)에서만 갱신해서 과도한 업데이트 방지
    - 마지막으로 처리한 지점 이후의 최근 변경분만 다시 요약해 비용과 지연을 줄임
    - Goal/Done 같은 고정 템플릿 대신, 작업 로그를 줄줄이 나열하지 않는 현재 상태 중심의 자연어 요약으로 표시
    - 생성된 제목을 session name 으로 저장
    - interactive 모드에서는 터미널 제목을 `π - <title>`로 동기화
    - 요약 스냅샷을 custom entry 로 저장해서 resume/tree 이동 후에도 즉시 복원
    - 터미널이 좁으면 자동으로 숨겨서 화면 침범을 줄임
