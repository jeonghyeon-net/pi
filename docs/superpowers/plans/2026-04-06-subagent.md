# Subagent Extension Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** pi-coding-agent용 서브에이전트 오케스트레이션 확장. 독립된 pi 서브프로세스를 비동기로 실행하고 followUp으로 결과를 전달한다.

**Architecture:** 도구의 execute 함수가 서브프로세스를 spawn하고 즉시 반환. 완료 시 pi.sendMessage(followUp)로 결과 전달. createTool(pi) 팩토리 패턴으로 pi 접근 (todo 확장과 동일). 16개 소스 파일 (99줄 제한 준수).

**Tech Stack:** TypeScript, TypeBox (파라미터 스키마), yargs-parser (CLI 파싱), vitest (테스트), pi-coding-agent ExtensionAPI

---

## File Structure

```
01_EXTENSIONS/subagent/
├── package.json
├── tsconfig.json
├── biome.json
├── vitest.config.ts
├── .gitignore
├── README
├── agents/
│   ├── scout.md
│   ├── worker.md
│   └── reviewer.md
├── src/
│   ├── index.ts        # entry (imports + pi.on + pi.registerTool)
│   ├── types.ts        # interfaces, SubagentPi, TypeBox schema
│   ├── constants.ts    # limits, markers
│   ├── agents.ts       # frontmatter parse + agent loading
│   ├── cli.ts          # command string parsing
│   ├── format.ts       # token/usage/tool formatting
│   ├── store.ts        # active runs state
│   ├── parser.ts       # JSON event line parsing
│   ├── retry.ts        # retry wrapper with backoff
│   ├── runner.ts       # pi subprocess spawn + management
│   ├── session.ts      # session file I/O + entry persistence
│   ├── context.ts      # main conversation context extraction
│   ├── execute.ts      # single/batch/chain orchestration
│   ├── widget.ts       # below-editor widget updates
│   ├── render.ts       # renderCall/renderResult
│   └── tool.ts         # tool factory (createTool)
├── dist/
│   └── index.js        # compiled output
└── tests/
    ├── types.test.ts
    ├── constants.test.ts
    ├── agents.test.ts
    ├── cli.test.ts
    ├── format.test.ts
    ├── store.test.ts
    ├── parser.test.ts
    ├── retry.test.ts
    ├── runner.test.ts
    ├── session.test.ts
    ├── context.test.ts
    ├── execute.test.ts
    ├── widget.test.ts
    ├── render.test.ts
    └── tool.test.ts
```

---

## Task 0: Architecture Test Updates

아키텍처 테스트를 subagent 확장에 맞게 업데이트한다. 현재 todo 확장도 test 13이 실패 중이므로 함께 수정.

**Files:**
- Modify: `00_ARCHITECTURE/tests/05_no_md_files_test.go`
- Modify: `00_ARCHITECTURE/tests/02_root_entries_test.go`
- Modify: `00_ARCHITECTURE/tests/04_max_lines_test.go`

- [ ] **Step 1: 확장 내 agents/*.md 허용 (test 05)**

```go
// 05_no_md_files_test.go - 기존 Walk 함수 내부에 추가
// SKILL.md 허용 블록 바로 아래에:
if strings.HasPrefix(rel, "01_EXTENSIONS/") && strings.Contains(rel, "/agents/") {
    return nil
}
```

- [ ] **Step 2: docs/ 디렉터리 허용 (test 02)**

`02_root_entries_test.go`의 허용 목록에 `"docs"` 추가.

- [ ] **Step 3: docs/ 99줄 제한 제외 (test 04)**

```go
// 04_max_lines_test.go - Walk 함수 내부 skip 블록에 추가
if strings.HasPrefix(rel, "docs/") {
    return filepath.SkipDir
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 00_ARCHITECTURE/tests && go test -run "TestAllFiles_NoMdExtension|TestRoot_AllowedEntries|TestAllFiles_MaxLines" -v`

- [ ] **Step 5: 커밋**

```bash
git add 00_ARCHITECTURE/tests/
git commit -m "아키텍처 테스트 업데이트: 확장 agents/*.md 허용, docs/ 디렉터리 추가"
```

---

## Task 1: Project Scaffolding

**Files:**
- Create: `01_EXTENSIONS/subagent/package.json`
- Create: `01_EXTENSIONS/subagent/tsconfig.json`
- Create: `01_EXTENSIONS/subagent/biome.json`
- Create: `01_EXTENSIONS/subagent/vitest.config.ts`
- Create: `01_EXTENSIONS/subagent/.gitignore`
- Create: `01_EXTENSIONS/subagent/README`

- [ ] **Step 1: package.json**

```json
{
  "pi": {
    "extensions": ["dist/index.js"]
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "yargs-parser": "^21.1.1"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0",
    "@vitest/coverage-v8": "^3.1.0"
  }
}
```

- [ ] **Step 2: tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "strict": true,
    "skipLibCheck": true
  },
  "include": ["src"]
}
```

- [ ] **Step 3: biome.json**

```json
{
  "formatter": {
    "indentStyle": "tab"
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  }
}
```

- [ ] **Step 4: vitest.config.ts**

```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
	test: {
		coverage: {
			include: ["src/**"],
			exclude: ["src/index.ts"],
			thresholds: {
				lines: 100,
				branches: 100,
				functions: 100,
				statements: 100,
			},
		},
	},
});
```

- [ ] **Step 5: .gitignore**

```
node_modules/
coverage/
```

- [ ] **Step 6: README**

```
subagent … 서브에이전트 오케스트레이션 (비동기 실행, 세션 관리)
```

- [ ] **Step 7: npm install + 빈 src/index.ts + dist/index.js**

```bash
cd 01_EXTENSIONS/subagent && npm install
mkdir -p src dist tests
echo 'export default function () {}' > src/index.ts
echo '"use strict";' > dist/index.js
```

- [ ] **Step 8: 커밋**

```bash
git add 01_EXTENSIONS/subagent/
git commit -m "subagent 확장 스캐폴딩"
```

---

## Task 2: types.ts

모든 인터페이스와 TypeBox 스키마를 정의한다.

**Files:**
- Create: `src/types.ts`
- Create: `tests/types.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import { SubagentParams } from "../src/types.js";
import { Value } from "@sinclair/typebox/value";

describe("types", () => {
	it("SubagentParams validates command string", () => {
		expect(Value.Check(SubagentParams, { command: "run scout -- find auth" })).toBe(true);
	});

	it("SubagentParams rejects missing command", () => {
		expect(Value.Check(SubagentParams, {})).toBe(false);
	});

	it("SubagentParams rejects non-string command", () => {
		expect(Value.Check(SubagentParams, { command: 123 })).toBe(false);
	});
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd 01_EXTENSIONS/subagent && npx vitest run tests/types.test.ts`
Expected: FAIL (module not found)

- [ ] **Step 3: 구현**

```typescript
import { Type } from "@sinclair/typebox";

export const SubagentParams = Type.Object({
	command: Type.String({ description: "Subcommand string (e.g. 'run scout -- find auth code')" }),
});

export interface AgentConfig {
	name: string;
	description: string;
	model?: string;
	thinking?: string;
	tools?: string[];
	systemPrompt: string;
	filePath: string;
}

export interface RunResult {
	id: number;
	agent: string;
	output: string;
	usage: UsageStats;
	escalation?: string;
	error?: string;
}

export interface UsageStats {
	inputTokens: number;
	outputTokens: number;
	turns: number;
}

export interface ActiveRun {
	id: number;
	agent: string;
	startedAt: number;
	abort: () => void;
}

export interface SubagentPi {
	sendMessage(
		msg: { customType: string; content: string; display: boolean },
		opts?: { deliverAs?: string; triggerTurn?: boolean },
	): void;
	appendEntry(type: string, data?: unknown): void;
}

export type Subcommand =
	| { type: "run"; agent: string; task: string; main: boolean; cwd?: string }
	| { type: "batch"; items: Array<{ agent: string; task: string }>; main: boolean }
	| { type: "chain"; steps: Array<{ agent: string; task: string }>; main: boolean }
	| { type: "continue"; id: number; task: string }
	| { type: "detail"; id: number }
	| { type: "runs" };
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 01_EXTENSIONS/subagent && npx vitest run tests/types.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/types.ts tests/types.test.ts
git commit -m "subagent types + TypeBox 스키마"
```

---

## Task 3: constants.ts

**Files:**
- Create: `src/constants.ts`
- Create: `tests/constants.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import * as C from "../src/constants.js";

describe("constants", () => {
	it("batch/concurrency limits are positive", () => {
		expect(C.MAX_BATCH_TASKS).toBeGreaterThan(0);
		expect(C.MAX_CONCURRENCY).toBeGreaterThan(0);
	});

	it("retry config is valid", () => {
		expect(C.MAX_RETRIES).toBeGreaterThan(0);
		expect(C.RETRY_BASE_MS).toBeGreaterThan(0);
	});

	it("escalation marker is non-empty", () => {
		expect(C.ESCALATION_MARKER.length).toBeGreaterThan(0);
	});

	it("pipeline max chars is positive", () => {
		expect(C.PIPELINE_MAX_CHARS).toBeGreaterThan(0);
	});

	it("queue interval is positive", () => {
		expect(C.QUEUE_INTERVAL_MS).toBeGreaterThan(0);
	});
});
```

- [ ] **Step 2: 구현**

```typescript
export const MAX_BATCH_TASKS = 16;
export const MAX_CONCURRENCY = 8;
export const QUEUE_INTERVAL_MS = 1000;
export const MAX_RETRIES = 3;
export const RETRY_BASE_MS = 2000;
export const ESCALATION_MARKER = "[ESCALATION]";
export const PIPELINE_MAX_CHARS = 4000;
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/constants.test.ts
git add src/constants.ts tests/constants.test.ts
git commit -m "subagent constants"
```

---

## Task 4: agents.ts

마크다운 파일에서 YAML 프론트매터를 파싱하여 에이전트 설정을 로드한다.

**Files:**
- Create: `src/agents.ts`
- Create: `tests/agents.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import { parseFrontmatter, loadAgentFromString, getAgent } from "../src/agents.js";

describe("parseFrontmatter", () => {
	it("parses yaml frontmatter and body", () => {
		const raw = "---\nname: scout\nmodel: gpt-5.4-mini\n---\nYou are a scout.";
		const { data, content } = parseFrontmatter(raw);
		expect(data.name).toBe("scout");
		expect(data.model).toBe("gpt-5.4-mini");
		expect(content).toBe("You are a scout.");
	});

	it("returns empty data when no frontmatter", () => {
		const { data, content } = parseFrontmatter("Just text");
		expect(data).toEqual({});
		expect(content).toBe("Just text");
	});

	it("handles tools as comma-separated list", () => {
		const raw = "---\ntools: read, grep, find\n---\nPrompt";
		const agent = loadAgentFromString(raw, "/fake/scout.md");
		expect(agent.tools).toEqual(["read", "grep", "find"]);
	});
});

describe("loadAgentFromString", () => {
	it("builds AgentConfig from raw markdown", () => {
		const raw = "---\nname: worker\ndescription: General worker\nmodel: gpt-5.4\nthinking: medium\n---\nDo work.";
		const agent = loadAgentFromString(raw, "/path/worker.md");
		expect(agent.name).toBe("worker");
		expect(agent.description).toBe("General worker");
		expect(agent.model).toBe("gpt-5.4");
		expect(agent.thinking).toBe("medium");
		expect(agent.systemPrompt).toBe("Do work.");
		expect(agent.filePath).toBe("/path/worker.md");
	});

	it("handles missing optional fields", () => {
		const raw = "---\nname: minimal\n---\nPrompt";
		const agent = loadAgentFromString(raw, "/path/minimal.md");
		expect(agent.model).toBeUndefined();
		expect(agent.thinking).toBeUndefined();
		expect(agent.tools).toBeUndefined();
	});
});

describe("getAgent", () => {
	const agents = [
		{ name: "scout", description: "", systemPrompt: "", filePath: "" },
		{ name: "worker", description: "", systemPrompt: "", filePath: "" },
	];

	it("finds agent by name", () => {
		expect(getAgent("scout", agents)?.name).toBe("scout");
	});

	it("returns undefined for missing agent", () => {
		expect(getAgent("unknown", agents)).toBeUndefined();
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import type { AgentConfig } from "./types.js";

export function parseFrontmatter(raw: string): { data: Record<string, string>; content: string } {
	const match = raw.match(/^---\n([\s\S]*?)\n---\n?([\s\S]*)$/);
	if (!match) return { data: {}, content: raw };
	const data: Record<string, string> = {};
	for (const line of match[1].split("\n")) {
		const idx = line.indexOf(":");
		if (idx > 0) data[line.slice(0, idx).trim()] = line.slice(idx + 1).trim();
	}
	return { data, content: match[2].trim() };
}

export function loadAgentFromString(raw: string, filePath: string): AgentConfig {
	const { data, content } = parseFrontmatter(raw);
	return {
		name: data.name ?? "",
		description: data.description ?? "",
		model: data.model || undefined,
		thinking: data.thinking || undefined,
		tools: data.tools ? data.tools.split(/,\s*/) : undefined,
		systemPrompt: content,
		filePath,
	};
}

export function loadAgentsFromDir(dir: string, readDir: typeof import("fs").readdirSync, readFile: typeof import("fs").readFileSync): AgentConfig[] {
	return readDir(dir)
		.filter((f: string) => f.endsWith(".md"))
		.map((f: string) => loadAgentFromString(readFile(`${dir}/${f}`, "utf-8"), `${dir}/${f}`));
}

export function getAgent(name: string, agents: Pick<AgentConfig, "name">[]): typeof agents[number] | undefined {
	return agents.find((a) => a.name === name);
}
```

참고: `loadAgentsFromDir`는 DI로 fs 함수를 받아 테스트 용이성 확보. 실제 호출은 tool.ts에서 실제 fs를 주입.

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/agents.test.ts
git add src/agents.ts tests/agents.test.ts
git commit -m "subagent agent 로딩 (frontmatter 파싱)"
```

---

## Task 5: cli.ts

커맨드 문자열을 Subcommand 타입으로 파싱한다.

**Files:**
- Create: `src/cli.ts`
- Create: `tests/cli.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import { parseCommand } from "../src/cli.js";

describe("parseCommand", () => {
	it("parses run command", () => {
		const cmd = parseCommand("run scout -- find auth code");
		expect(cmd).toEqual({ type: "run", agent: "scout", task: "find auth code", main: false, cwd: undefined });
	});

	it("parses run with --main", () => {
		const cmd = parseCommand("run worker --main -- implement login");
		expect(cmd).toEqual({ type: "run", agent: "worker", task: "implement login", main: true, cwd: undefined });
	});

	it("parses run with --cwd", () => {
		const cmd = parseCommand("run worker --cwd /tmp -- task");
		expect(cmd.type === "run" && cmd.cwd).toBe("/tmp");
	});

	it("parses batch", () => {
		const cmd = parseCommand("batch --agent worker --task taskA --agent reviewer --task taskB");
		expect(cmd).toEqual({
			type: "batch",
			items: [{ agent: "worker", task: "taskA" }, { agent: "reviewer", task: "taskB" }],
			main: false,
		});
	});

	it("parses chain", () => {
		const cmd = parseCommand("chain --agent scout --task find --agent worker --task impl");
		expect(cmd).toEqual({
			type: "chain",
			steps: [{ agent: "scout", task: "find" }, { agent: "worker", task: "impl" }],
			main: false,
		});
	});

	it("parses continue", () => {
		const cmd = parseCommand("continue 3 -- add error handling");
		expect(cmd).toEqual({ type: "continue", id: 3, task: "add error handling" });
	});

	it("parses detail", () => {
		expect(parseCommand("detail 5")).toEqual({ type: "detail", id: 5 });
	});

	it("parses runs", () => {
		expect(parseCommand("runs")).toEqual({ type: "runs" });
	});

	it("throws on unknown subcommand", () => {
		expect(() => parseCommand("unknown")).toThrow("Unknown subcommand");
	});

	it("parses batch with --main", () => {
		const cmd = parseCommand("batch --main --agent w --task t");
		expect(cmd.type === "batch" && cmd.main).toBe(true);
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import yargsParser from "yargs-parser";
import type { Subcommand } from "./types.js";

function toArray(val: unknown): string[] {
	if (Array.isArray(val)) return val.map(String);
	if (val !== undefined) return [String(val)];
	return [];
}

function zipAgentTask(argv: yargsParser.Arguments): Array<{ agent: string; task: string }> {
	const agents = toArray(argv.agent);
	const tasks = toArray(argv.task);
	return agents.map((a, i) => ({ agent: a, task: tasks[i] ?? "" }));
}

export function parseCommand(command: string): Subcommand {
	const [head, ...rest] = command.split(" -- ");
	const task = rest.join(" -- ").trim();
	const argv = yargsParser(head);
	const sub = String(argv._[0] ?? "");

	switch (sub) {
		case "run":
			return { type: "run", agent: String(argv._[1] ?? ""), task, main: Boolean(argv.main), cwd: argv.cwd ? String(argv.cwd) : undefined };
		case "batch":
			return { type: "batch", items: zipAgentTask(argv), main: Boolean(argv.main) };
		case "chain":
			return { type: "chain", steps: zipAgentTask(argv), main: Boolean(argv.main) };
		case "continue":
			return { type: "continue", id: Number(argv._[1]), task };
		case "detail":
			return { type: "detail", id: Number(argv._[1]) };
		case "runs":
			return { type: "runs" };
		default:
			throw new Error(`Unknown subcommand: ${sub}`);
	}
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/cli.test.ts
git add src/cli.ts tests/cli.test.ts
git commit -m "subagent CLI 파서"
```

---

## Task 6: format.ts

토큰, 사용량, 도구 호출 포맷팅 유틸리티.

**Files:**
- Create: `src/format.ts`
- Create: `tests/format.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import { formatTokens, formatUsage, formatDuration } from "../src/format.js";

describe("formatTokens", () => {
	it("formats small numbers", () => expect(formatTokens(500)).toBe("500"));
	it("formats thousands", () => expect(formatTokens(12500)).toBe("12.5k"));
	it("formats millions", () => expect(formatTokens(1200000)).toBe("1.2M"));
	it("handles zero", () => expect(formatTokens(0)).toBe("0"));
});

describe("formatUsage", () => {
	it("formats usage stats", () => {
		const s = formatUsage({ inputTokens: 1000, outputTokens: 500, turns: 3 });
		expect(s).toContain("1k");
		expect(s).toContain("500");
		expect(s).toContain("3");
	});
});

describe("formatDuration", () => {
	it("formats seconds", () => expect(formatDuration(5000)).toBe("5s"));
	it("formats minutes", () => expect(formatDuration(125000)).toBe("2m 5s"));
	it("handles zero", () => expect(formatDuration(0)).toBe("0s"));
});
```

- [ ] **Step 2: 구현**

```typescript
import type { UsageStats } from "./types.js";

export function formatTokens(n: number): string {
	if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M`;
	if (n >= 1_000) return `${(n / 1_000).toFixed(1)}k`;
	return String(n);
}

export function formatUsage(stats: UsageStats): string {
	return `${formatTokens(stats.inputTokens)} in / ${formatTokens(stats.outputTokens)} out / ${stats.turns} turns`;
}

export function formatDuration(ms: number): string {
	const sec = Math.floor(ms / 1000);
	if (sec < 60) return `${sec}s`;
	return `${Math.floor(sec / 60)}m ${sec % 60}s`;
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/format.test.ts
git add src/format.ts tests/format.test.ts
git commit -m "subagent format 유틸"
```

---

## Task 7: store.ts

실행 중인 서브에이전트 상태 관리. 모듈 레벨 상태.

**Files:**
- Create: `src/store.ts`
- Create: `tests/store.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import { addRun, getRun, removeRun, listRuns, nextId, resetStore, completeRun, getCompleted, drainCompleted } from "../src/store.js";

describe("store", () => {
	beforeEach(() => resetStore());

	it("adds and retrieves run", () => {
		const id = nextId();
		addRun({ id, agent: "scout", startedAt: Date.now(), abort: () => {} });
		expect(getRun(id)?.agent).toBe("scout");
	});

	it("increments id", () => {
		expect(nextId()).toBe(1);
		expect(nextId()).toBe(2);
	});

	it("removes run", () => {
		const id = nextId();
		addRun({ id, agent: "scout", startedAt: 0, abort: () => {} });
		removeRun(id);
		expect(getRun(id)).toBeUndefined();
	});

	it("lists active runs", () => {
		addRun({ id: nextId(), agent: "a", startedAt: 0, abort: () => {} });
		addRun({ id: nextId(), agent: "b", startedAt: 0, abort: () => {} });
		expect(listRuns()).toHaveLength(2);
	});

	it("tracks completed results", () => {
		completeRun(1, { id: 1, agent: "scout", output: "done", usage: { inputTokens: 0, outputTokens: 0, turns: 0 } });
		expect(getCompleted()).toHaveLength(1);
	});

	it("drainCompleted returns and clears", () => {
		completeRun(1, { id: 1, agent: "w", output: "ok", usage: { inputTokens: 0, outputTokens: 0, turns: 0 } });
		expect(drainCompleted()).toHaveLength(1);
		expect(drainCompleted()).toHaveLength(0);
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import type { ActiveRun, RunResult } from "./types.js";

let counter = 0;
const active = new Map<number, ActiveRun>();
const completed: RunResult[] = [];

export function nextId(): number { return ++counter; }
export function addRun(run: ActiveRun): void { active.set(run.id, run); }
export function getRun(id: number): ActiveRun | undefined { return active.get(id); }
export function removeRun(id: number): void { active.delete(id); }
export function listRuns(): ActiveRun[] { return [...active.values()]; }
export function completeRun(id: number, result: RunResult): void { completed.push(result); }
export function getCompleted(): RunResult[] { return [...completed]; }
export function drainCompleted(): RunResult[] { return completed.splice(0); }

export function resetStore(): void {
	counter = 0;
	active.clear();
	completed.length = 0;
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/store.test.ts
git add src/store.ts tests/store.test.ts
git commit -m "subagent store (실행 상태 관리)"
```

---

## Task 8: parser.ts

pi --mode json의 stdout에서 나오는 JSON 이벤트를 파싱한다.

**Files:**
- Create: `src/parser.ts`
- Create: `tests/parser.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import { parseLine } from "../src/parser.js";

describe("parseLine", () => {
	it("parses message_end with assistant role", () => {
		const event = parseLine(JSON.stringify({
			type: "message_end",
			message: { role: "assistant", content: [{ type: "text", text: "Hello" }], usage: { inputTokens: 10, outputTokens: 5 } },
		}));
		expect(event?.type).toBe("message");
		expect(event?.text).toBe("Hello");
		expect(event?.usage?.inputTokens).toBe(10);
	});

	it("ignores non-assistant message_end", () => {
		const event = parseLine(JSON.stringify({
			type: "message_end",
			message: { role: "user", content: [{ type: "text", text: "Hi" }] },
		}));
		expect(event).toBeNull();
	});

	it("parses tool_execution_start", () => {
		const event = parseLine(JSON.stringify({
			type: "tool_execution_start",
			toolName: "read",
			args: { path: "/test" },
		}));
		expect(event?.type).toBe("tool_start");
		expect(event?.toolName).toBe("read");
	});

	it("parses tool_execution_end", () => {
		const event = parseLine(JSON.stringify({ type: "tool_execution_end", toolName: "read" }));
		expect(event?.type).toBe("tool_end");
	});

	it("parses agent_end", () => {
		const event = parseLine(JSON.stringify({ type: "agent_end", messages: [] }));
		expect(event?.type).toBe("agent_end");
	});

	it("returns null for unknown event", () => {
		expect(parseLine(JSON.stringify({ type: "turn_start" }))).toBeNull();
	});

	it("returns null for invalid JSON", () => {
		expect(parseLine("not json")).toBeNull();
	});

	it("returns null for empty line", () => {
		expect(parseLine("")).toBeNull();
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import type { UsageStats } from "./types.js";

export interface ParsedEvent {
	type: "message" | "tool_start" | "tool_end" | "agent_end";
	text?: string;
	usage?: Partial<UsageStats>;
	toolName?: string;
}

export function parseLine(line: string): ParsedEvent | null {
	if (!line.trim()) return null;
	try {
		const evt = JSON.parse(line);
		switch (evt.type) {
			case "message_end": return parseMessageEnd(evt);
			case "tool_execution_start": return { type: "tool_start", toolName: evt.toolName };
			case "tool_execution_end": return { type: "tool_end", toolName: evt.toolName };
			case "agent_end": return { type: "agent_end" };
			default: return null;
		}
	} catch { return null; }
}

function parseMessageEnd(evt: { message?: { role?: string; content?: Array<{ type: string; text?: string }>; usage?: Record<string, number> } }): ParsedEvent | null {
	const msg = evt.message;
	if (!msg || msg.role !== "assistant") return null;
	const text = msg.content?.filter((c) => c.type === "text").map((c) => c.text).join("\n") ?? "";
	const usage = msg.usage ? { inputTokens: msg.usage.inputTokens ?? 0, outputTokens: msg.usage.outputTokens ?? 0, turns: 1 } : undefined;
	return { type: "message", text, usage };
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/parser.test.ts
git add src/parser.ts tests/parser.test.ts
git commit -m "subagent JSON 이벤트 파서"
```

---

## Task 9: retry.ts

일시적 실패 감지 + 지수 백오프 재시도.

**Files:**
- Create: `src/retry.ts`
- Create: `tests/retry.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect, vi } from "vitest";
import { isTransient, withRetry } from "../src/retry.js";

describe("isTransient", () => {
	it("detects network errors", () => {
		expect(isTransient(new Error("ECONNRESET"))).toBe(true);
		expect(isTransient(new Error("ETIMEDOUT"))).toBe(true);
	});

	it("detects rate limit", () => {
		expect(isTransient(new Error("429 Too Many Requests"))).toBe(true);
	});

	it("detects 5xx", () => {
		expect(isTransient(new Error("502 Bad Gateway"))).toBe(true);
	});

	it("rejects permanent errors", () => {
		expect(isTransient(new Error("404 Not Found"))).toBe(false);
		expect(isTransient(new Error("Invalid API key"))).toBe(false);
	});
});

describe("withRetry", () => {
	it("returns on first success", async () => {
		const fn = vi.fn().mockResolvedValue("ok");
		expect(await withRetry(fn, 3, 1)).toBe("ok");
		expect(fn).toHaveBeenCalledTimes(1);
	});

	it("retries on transient failure", async () => {
		const fn = vi.fn()
			.mockRejectedValueOnce(new Error("ECONNRESET"))
			.mockResolvedValue("ok");
		expect(await withRetry(fn, 3, 1)).toBe("ok");
		expect(fn).toHaveBeenCalledTimes(2);
	});

	it("throws after max retries", async () => {
		const fn = vi.fn().mockRejectedValue(new Error("ECONNRESET"));
		await expect(withRetry(fn, 2, 1)).rejects.toThrow("ECONNRESET");
		expect(fn).toHaveBeenCalledTimes(3);
	});

	it("throws immediately on permanent error", async () => {
		const fn = vi.fn().mockRejectedValue(new Error("Invalid key"));
		await expect(withRetry(fn, 3, 1)).rejects.toThrow("Invalid key");
		expect(fn).toHaveBeenCalledTimes(1);
	});
});
```

- [ ] **Step 2: 구현**

```typescript
const TRANSIENT_PATTERNS = [/ECONNRESET/, /ETIMEDOUT/, /ENOTFOUND/, /429/, /5\d{2}/];

export function isTransient(err: Error): boolean {
	return TRANSIENT_PATTERNS.some((p) => p.test(err.message));
}

export async function withRetry<T>(fn: () => Promise<T>, maxRetries: number, baseMs: number): Promise<T> {
	let lastErr: Error | undefined;
	for (let attempt = 0; attempt <= maxRetries; attempt++) {
		try { return await fn(); }
		catch (err) {
			lastErr = err instanceof Error ? err : new Error(String(err));
			if (!isTransient(lastErr) || attempt === maxRetries) throw lastErr;
			await new Promise((r) => setTimeout(r, baseMs * 2 ** attempt));
		}
	}
	throw lastErr;
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/retry.test.ts
git add src/retry.ts tests/retry.test.ts
git commit -m "subagent retry 로직"
```

---

## Task 10: runner.ts

pi 서브프로세스를 spawn하고 JSON 이벤트를 수집하여 RunResult를 반환한다.

**Files:**
- Create: `src/runner.ts`
- Create: `tests/runner.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect, vi } from "vitest";
import { buildArgs, collectOutput, getPiCommand } from "../src/runner.js";

describe("getPiCommand", () => {
	it("uses process.execPath when argv[1] exists", () => {
		const result = getPiCommand("/usr/bin/node", "/path/to/pi.js", (p: string) => p === "/path/to/pi.js");
		expect(result.cmd).toBe("/usr/bin/node");
		expect(result.base).toEqual(["/path/to/pi.js"]);
	});

	it("falls back to 'pi' for generic runtime", () => {
		const result = getPiCommand("/usr/bin/node", "/nonexistent", () => false);
		expect(result.cmd).toBe("pi");
		expect(result.base).toEqual([]);
	});
});

describe("buildArgs", () => {
	it("builds args for simple run", () => {
		const args = buildArgs({
			base: [], model: "gpt-5.4", tools: ["read", "grep"],
			systemPromptPath: "/tmp/prompt.md", task: "find auth", sessionPath: undefined,
		});
		expect(args).toContain("--mode");
		expect(args).toContain("json");
		expect(args).toContain("--no-session");
		expect(args).toContain("--model");
		expect(args).toContain("gpt-5.4");
		expect(args).toContain("--tools");
		expect(args).toContain("read,grep");
	});

	it("uses --session when sessionPath provided", () => {
		const args = buildArgs({
			base: [], model: undefined, tools: undefined,
			systemPromptPath: "/tmp/p.md", task: "t", sessionPath: "/tmp/s.json",
		});
		expect(args).toContain("--session");
		expect(args).not.toContain("--no-session");
	});
});

describe("collectOutput", () => {
	it("aggregates text from message events", () => {
		const result = collectOutput([
			{ type: "message", text: "Hello", usage: { inputTokens: 10, outputTokens: 5, turns: 1 } },
			{ type: "tool_start", toolName: "read" },
			{ type: "message", text: " World", usage: { inputTokens: 20, outputTokens: 10, turns: 1 } },
			{ type: "agent_end" },
		]);
		expect(result.output).toBe("Hello\n World");
		expect(result.usage.inputTokens).toBe(30);
		expect(result.usage.outputTokens).toBe(15);
		expect(result.usage.turns).toBe(2);
	});

	it("returns empty for no messages", () => {
		const result = collectOutput([{ type: "agent_end" }]);
		expect(result.output).toBe("");
		expect(result.usage.turns).toBe(0);
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import type { AgentConfig, UsageStats } from "./types.js";
import type { ParsedEvent } from "./parser.js";
import { ESCALATION_MARKER } from "./constants.js";

export function getPiCommand(execPath: string, argv1: string, exists: (p: string) => boolean): { cmd: string; base: string[] } {
	if (argv1 && exists(argv1)) return { cmd: execPath, base: [argv1] };
	return { cmd: "pi", base: [] };
}

export interface BuildArgsInput {
	base: string[]; model?: string; tools?: string[];
	systemPromptPath: string; task: string; sessionPath?: string;
}

export function buildArgs(input: BuildArgsInput): string[] {
	const args = [...input.base, "--mode", "json", "-p"];
	if (input.sessionPath) { args.push("--session", input.sessionPath); }
	else { args.push("--no-session"); }
	if (input.model) { args.push("--model", input.model); }
	if (input.tools) { args.push("--tools", input.tools.join(",")); }
	args.push("--append-system-prompt", input.systemPromptPath);
	args.push(`Task: ${input.task}`);
	return args;
}

export function collectOutput(events: ParsedEvent[]): { output: string; usage: UsageStats; escalation?: string } {
	const texts: string[] = [];
	const usage: UsageStats = { inputTokens: 0, outputTokens: 0, turns: 0 };
	for (const evt of events) {
		if (evt.type === "message" && evt.text) {
			texts.push(evt.text);
			usage.inputTokens += evt.usage?.inputTokens ?? 0;
			usage.outputTokens += evt.usage?.outputTokens ?? 0;
			usage.turns += evt.usage?.turns ?? 0;
		}
	}
	const output = texts.join("\n");
	const escalation = output.includes(ESCALATION_MARKER) ? output.split(ESCALATION_MARKER)[1]?.trim() : undefined;
	return { output, usage, escalation };
}
```

참고: 실제 `runAgent` 함수 (spawn + readline)는 execute.ts에서 조립. runner.ts는 순수 함수들만 export하여 테스트 용이성 확보.

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/runner.test.ts
git add src/runner.ts tests/runner.test.ts
git commit -m "subagent runner 헬퍼 (args 빌드, 출력 수집)"
```

---

## Task 11: session.ts

세션 파일 경로 관리 + entry 기반 상태 영속화.

**Files:**
- Create: `src/session.ts`
- Create: `tests/session.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import { sessionPath, buildRunsEntry, restoreRuns, getRunHistory, resetSession } from "../src/session.js";

describe("sessionPath", () => {
	it("generates path from id", () => {
		const p = sessionPath(1, "/home/user");
		expect(p).toContain("subagents");
		expect(p).toContain("1");
		expect(p).toMatch(/\.json$/);
	});
});

describe("entry persistence", () => {
	beforeEach(() => resetSession());

	it("buildRunsEntry captures history", () => {
		restoreRuns([]);
		const entry = buildRunsEntry();
		expect(entry).toHaveProperty("runs");
		expect(entry).toHaveProperty("updatedAt");
	});

	it("restoreRuns from custom entries", () => {
		const entries = [{
			type: "custom",
			customType: "subagent-runs",
			data: { runs: [{ id: 1, agent: "scout", output: "ok", sessionFile: "/tmp/1.json" }], updatedAt: 0 },
		}];
		restoreRuns(entries);
		expect(getRunHistory()).toHaveLength(1);
		expect(getRunHistory()[0].agent).toBe("scout");
	});

	it("takes last entry", () => {
		const entries = [
			{ type: "custom", customType: "subagent-runs", data: { runs: [{ id: 1, agent: "a" }], updatedAt: 0 } },
			{ type: "custom", customType: "subagent-runs", data: { runs: [{ id: 2, agent: "b" }], updatedAt: 1 } },
		];
		restoreRuns(entries);
		expect(getRunHistory()[0].agent).toBe("b");
	});

	it("skips non-subagent entries", () => {
		restoreRuns([{ type: "custom", customType: "other" }, { type: "message" }]);
		expect(getRunHistory()).toEqual([]);
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import { join } from "path";
import { homedir } from "os";

interface RunHistoryItem {
	id: number;
	agent: string;
	output?: string;
	sessionFile?: string;
}

let history: RunHistoryItem[] = [];

export function sessionPath(id: number, home?: string): string {
	return join(home ?? homedir(), ".pi", "agent", "sessions", "subagents", `run-${id}.json`);
}

export function addToHistory(item: RunHistoryItem): void { history.push(item); }
export function getRunHistory(): RunHistoryItem[] { return [...history]; }

export function buildRunsEntry(): { runs: RunHistoryItem[]; updatedAt: number } {
	return { runs: [...history], updatedAt: Date.now() };
}

export function restoreRuns(entries: Array<Record<string, unknown>>): void {
	const relevant = entries.filter((e) => e.type === "custom" && e.customType === "subagent-runs");
	const last = relevant.at(-1);
	if (last?.data && typeof last.data === "object" && "runs" in (last.data as object)) {
		history = [...((last.data as { runs: RunHistoryItem[] }).runs)];
	} else { history = []; }
}

export function resetSession(): void { history = []; }
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/session.test.ts
git add src/session.ts tests/session.test.ts
git commit -m "subagent session 관리"
```

---

## Task 12: context.ts

메인 대화 컨텍스트를 추출하여 서브에이전트에 주입할 텍스트를 생성한다.

**Files:**
- Create: `src/context.ts`
- Create: `tests/context.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import { extractMainContext } from "../src/context.js";

describe("extractMainContext", () => {
	it("extracts recent messages", () => {
		const entries = [
			{ type: "message", message: { role: "user", content: [{ type: "text", text: "Hello" }] } },
			{ type: "message", message: { role: "assistant", content: [{ type: "text", text: "Hi" }] } },
		];
		const ctx = extractMainContext(entries, 20);
		expect(ctx).toContain("Hello");
		expect(ctx).toContain("Hi");
	});

	it("limits to maxMessages", () => {
		const entries = Array.from({ length: 30 }, (_, i) => ({
			type: "message", message: { role: "user", content: [{ type: "text", text: `msg${i}` }] },
		}));
		const ctx = extractMainContext(entries, 5);
		expect(ctx).toContain("msg29");
		expect(ctx).not.toContain("msg0");
	});

	it("includes compaction summary", () => {
		const entries = [
			{ type: "compaction", summary: "Previous context summary" },
			{ type: "message", message: { role: "user", content: [{ type: "text", text: "New" }] } },
		];
		const ctx = extractMainContext(entries, 20);
		expect(ctx).toContain("Previous context summary");
	});

	it("returns empty for no entries", () => {
		expect(extractMainContext([], 20)).toBe("");
	});
});
```

- [ ] **Step 2: 구현**

```typescript
interface Entry {
	type: string;
	message?: { role: string; content: Array<{ type: string; text?: string }> };
	summary?: string;
}

function extractText(entry: Entry): string {
	if (!entry.message?.content) return "";
	return entry.message.content.filter((c) => c.type === "text").map((c) => c.text ?? "").join("\n");
}

export function extractMainContext(entries: Array<Record<string, unknown>>, maxMessages: number): string {
	const typed = entries as unknown as Entry[];
	const parts: string[] = [];
	const compaction = typed.find((e) => e.type === "compaction");
	if (compaction?.summary) parts.push(`[Context Summary]\n${compaction.summary}`);

	const messages = typed.filter((e) => e.type === "message" && e.message);
	const recent = messages.slice(-maxMessages);
	for (const entry of recent) {
		const role = entry.message?.role ?? "unknown";
		const text = extractText(entry);
		if (text) parts.push(`[${role}] ${text}`);
	}
	return parts.join("\n\n");
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/context.test.ts
git add src/context.ts tests/context.test.ts
git commit -m "subagent 메인 컨텍스트 추출"
```

---

## Task 13: execute.ts

single/batch/chain 오케스트레이션. 서브프로세스를 spawn하고 비동기로 결과를 전달한다.

**Files:**
- Create: `src/execute.ts`
- Create: `tests/execute.test.ts`

이 모듈은 실제 서브프로세스를 spawn하므로 DI로 runner를 주입받는다.

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect, vi } from "vitest";
import { executeSingle, executeBatch, executeChain } from "../src/execute.js";
import type { RunResult, AgentConfig } from "../src/types.js";

const agent: AgentConfig = { name: "scout", description: "", model: "gpt-5.4-mini", systemPrompt: "find code", filePath: "/agents/scout.md" };
const ok: RunResult = { id: 1, agent: "scout", output: "found auth.ts", usage: { inputTokens: 100, outputTokens: 50, turns: 2 } };

describe("executeSingle", () => {
	it("runs agent and returns result", async () => {
		const runner = vi.fn().mockResolvedValue(ok);
		const result = await executeSingle(agent, "find auth", { runner });
		expect(runner).toHaveBeenCalledOnce();
		expect(result.output).toBe("found auth.ts");
	});
});

describe("executeBatch", () => {
	it("runs items in parallel up to concurrency", async () => {
		const runner = vi.fn().mockResolvedValue(ok);
		const agents = [agent, { ...agent, name: "worker" }];
		const items = [{ agent: "scout", task: "A" }, { agent: "worker", task: "B" }];
		const results = await executeBatch(items, agents, { runner, concurrency: 2 });
		expect(results).toHaveLength(2);
		expect(runner).toHaveBeenCalledTimes(2);
	});

	it("handles partial failures", async () => {
		const runner = vi.fn()
			.mockResolvedValueOnce(ok)
			.mockRejectedValueOnce(new Error("fail"));
		const items = [{ agent: "scout", task: "A" }, { agent: "scout", task: "B" }];
		const results = await executeBatch(items, [agent], { runner, concurrency: 2 });
		expect(results[0].output).toBe("found auth.ts");
		expect(results[1].error).toBe("fail");
	});
});

describe("executeChain", () => {
	it("passes previous output to next step", async () => {
		const runner = vi.fn()
			.mockResolvedValueOnce({ ...ok, output: "step1 result" })
			.mockResolvedValueOnce({ ...ok, output: "step2 done" });
		const steps = [
			{ agent: "scout", task: "find" },
			{ agent: "worker", task: "implement {previous}" },
		];
		const result = await executeChain(steps, [agent, { ...agent, name: "worker" }], { runner });
		expect(runner).toHaveBeenCalledTimes(2);
		expect(runner.mock.calls[1][1]).toContain("step1 result");
		expect(result.output).toBe("step2 done");
	});

	it("stops on escalation", async () => {
		const escalated = { ...ok, output: "need help", escalation: "what to do?" };
		const runner = vi.fn().mockResolvedValue(escalated);
		const steps = [{ agent: "scout", task: "find" }, { agent: "worker", task: "impl" }];
		const result = await executeChain(steps, [agent, { ...agent, name: "worker" }], { runner });
		expect(runner).toHaveBeenCalledTimes(1);
		expect(result.escalation).toBe("what to do?");
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import type { RunResult, AgentConfig } from "./types.js";
import { getAgent } from "./agents.js";
import { PIPELINE_MAX_CHARS, MAX_CONCURRENCY } from "./constants.js";

type RunnerFn = (agent: AgentConfig, task: string) => Promise<RunResult>;
interface ExecOpts { runner: RunnerFn; concurrency?: number }

export async function executeSingle(agent: AgentConfig, task: string, opts: ExecOpts): Promise<RunResult> {
	return opts.runner(agent, task);
}

export async function executeBatch(
	items: Array<{ agent: string; task: string }>,
	agents: AgentConfig[],
	opts: ExecOpts,
): Promise<RunResult[]> {
	const limit = opts.concurrency ?? MAX_CONCURRENCY;
	const results: RunResult[] = [];
	const queue = [...items];
	const running: Promise<void>[] = [];
	for (const item of queue) {
		const agent = getAgent(item.agent, agents);
		if (!agent) { results.push({ id: 0, agent: item.agent, output: "", usage: { inputTokens: 0, outputTokens: 0, turns: 0 }, error: `Unknown agent: ${item.agent}` }); continue; }
		const p = opts.runner(agent, item.task)
			.then((r) => { results.push(r); })
			.catch((e) => { results.push({ id: 0, agent: item.agent, output: "", usage: { inputTokens: 0, outputTokens: 0, turns: 0 }, error: e.message }); });
		running.push(p);
		if (running.length >= limit) { await Promise.race(running); }
	}
	await Promise.all(running);
	return results;
}

export async function executeChain(
	steps: Array<{ agent: string; task: string }>,
	agents: AgentConfig[],
	opts: ExecOpts,
): Promise<RunResult> {
	let previous = "";
	let lastResult: RunResult = { id: 0, agent: "", output: "", usage: { inputTokens: 0, outputTokens: 0, turns: 0 } };
	for (const step of steps) {
		const agent = getAgent(step.agent, agents);
		if (!agent) return { ...lastResult, error: `Unknown agent: ${step.agent}` };
		const task = step.task.replace("{previous}", previous.slice(0, PIPELINE_MAX_CHARS));
		lastResult = await opts.runner(agent, task);
		if (lastResult.escalation) return lastResult;
		previous = lastResult.output;
	}
	return lastResult;
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/execute.test.ts
git add src/execute.ts tests/execute.test.ts
git commit -m "subagent execute 오케스트레이션 (single/batch/chain)"
```

---

## Task 14: widget.ts

에디터 하단 위젯 업데이트 헬퍼.

**Files:**
- Create: `src/widget.ts`
- Create: `tests/widget.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect, vi } from "vitest";
import { buildWidgetLines, syncWidget } from "../src/widget.js";

describe("buildWidgetLines", () => {
	it("shows running agents", () => {
		const runs = [
			{ id: 1, agent: "scout", startedAt: Date.now() - 5000 },
			{ id: 2, agent: "worker", startedAt: Date.now() - 10000 },
		];
		const lines = buildWidgetLines(runs, Date.now());
		expect(lines).toHaveLength(2);
		expect(lines[0]).toContain("scout");
		expect(lines[0]).toContain("#1");
	});

	it("returns empty for no runs", () => {
		expect(buildWidgetLines([], Date.now())).toEqual([]);
	});

	it("limits to 3 visible", () => {
		const runs = Array.from({ length: 5 }, (_, i) => ({ id: i, agent: "w", startedAt: 0 }));
		expect(buildWidgetLines(runs, Date.now()).length).toBeLessThanOrEqual(3);
	});
});

describe("syncWidget", () => {
	it("sets widget when runs exist", () => {
		const setWidget = vi.fn();
		const ctx = { hasUI: true, ui: { setWidget } };
		syncWidget(ctx, [{ id: 1, agent: "scout", startedAt: Date.now() }]);
		expect(setWidget).toHaveBeenCalledWith("subagent-status", expect.any(Array), { placement: "belowEditor" });
	});

	it("clears widget when no runs", () => {
		const setWidget = vi.fn();
		syncWidget({ hasUI: true, ui: { setWidget } }, []);
		expect(setWidget).toHaveBeenCalledWith("subagent-status", undefined);
	});

	it("skips when no UI", () => {
		const setWidget = vi.fn();
		syncWidget({ hasUI: false, ui: { setWidget } }, [{ id: 1, agent: "w", startedAt: 0 }]);
		expect(setWidget).not.toHaveBeenCalled();
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import { formatDuration } from "./format.js";

const MAX_VISIBLE = 3;

interface MinimalRun { id: number; agent: string; startedAt: number }
interface MinimalCtx { hasUI: boolean; ui: { setWidget: (key: string, content: string[] | undefined, opts?: { placement: string }) => void } }

export function buildWidgetLines(runs: MinimalRun[], now: number): string[] {
	return runs.slice(0, MAX_VISIBLE).map((r) => {
		const elapsed = formatDuration(now - r.startedAt);
		return `⟳ ${r.agent} #${r.id} (${elapsed})`;
	});
}

export function syncWidget(ctx: MinimalCtx, runs: MinimalRun[]): void {
	if (!ctx.hasUI) return;
	if (runs.length === 0) { ctx.ui.setWidget("subagent-status", undefined); return; }
	ctx.ui.setWidget("subagent-status", buildWidgetLines(runs, Date.now()), { placement: "belowEditor" });
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/widget.test.ts
git add src/widget.ts tests/widget.test.ts
git commit -m "subagent widget"
```

---

## Task 15: render.ts

renderCall (도구 호출 표시) + renderResult (결과 표시) 컴포넌트.

**Files:**
- Create: `src/render.ts`
- Create: `tests/render.test.ts`

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect } from "vitest";
import { buildCallText, buildResultText } from "../src/render.js";

describe("buildCallText", () => {
	it("shows run command", () => {
		expect(buildCallText({ command: "run scout -- find auth" })).toContain("scout");
	});

	it("shows batch count", () => {
		expect(buildCallText({ command: "batch --agent w --task a --agent r --task b" })).toContain("2");
	});

	it("shows chain steps", () => {
		expect(buildCallText({ command: "chain --agent s --task a --agent w --task b" })).toContain("chain");
	});
});

describe("buildResultText", () => {
	it("formats success", () => {
		const text = buildResultText({ id: 1, agent: "scout", output: "found it", usage: { inputTokens: 100, outputTokens: 50, turns: 2 } });
		expect(text).toContain("scout #1");
		expect(text).toContain("found it");
	});

	it("formats error", () => {
		const text = buildResultText({ id: 1, agent: "worker", output: "", error: "crashed", usage: { inputTokens: 0, outputTokens: 0, turns: 0 } });
		expect(text).toContain("error");
		expect(text).toContain("crashed");
	});

	it("formats escalation", () => {
		const text = buildResultText({ id: 1, agent: "worker", output: "", escalation: "delete file?", usage: { inputTokens: 0, outputTokens: 0, turns: 0 } });
		expect(text).toContain("escalation");
		expect(text).toContain("delete file?");
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import type { RunResult } from "./types.js";
import { formatUsage } from "./format.js";
import { parseCommand } from "./cli.js";

export function buildCallText(params: { command: string }): string {
	try {
		const cmd = parseCommand(params.command);
		if (cmd.type === "run") return `▶ ${cmd.agent}: ${cmd.task}`;
		if (cmd.type === "batch") return `▶ batch (${cmd.items.length} tasks)`;
		if (cmd.type === "chain") return `▶ chain (${cmd.steps.length} steps)`;
		if (cmd.type === "continue") return `▶ continue #${cmd.id}: ${cmd.task}`;
		if (cmd.type === "detail") return `▶ detail #${cmd.id}`;
		return `▶ ${params.command}`;
	} catch { return `▶ ${params.command}`; }
}

export function buildResultText(result: RunResult): string {
	const header = `${result.agent} #${result.id}`;
	if (result.error) return `✗ ${header} error: ${result.error}`;
	if (result.escalation) return `⚠ ${header} escalation: ${result.escalation}`;
	return `✓ ${header}\n${result.output}\n\n${formatUsage(result.usage)}`;
}
```

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/render.test.ts
git add src/render.ts tests/render.test.ts
git commit -m "subagent render (call/result 텍스트)"
```

---

## Task 16: tool.ts

createTool 팩토리. SubagentPi를 받아 도구 정의를 반환한다.

**Files:**
- Create: `src/tool.ts`
- Create: `tests/tool.test.ts`

이 모듈은 전체 흐름을 조립한다: CLI 파싱 → 에이전트 로딩 → execute 호출 → followUp 전달.

- [ ] **Step 1: 테스트 작성**

```typescript
import { describe, it, expect, vi } from "vitest";
import { createTool } from "../src/tool.js";

function stubPi() {
	return { sendMessage: vi.fn(), appendEntry: vi.fn() };
}

function stubCtx() {
	return { hasUI: true, ui: { setWidget: vi.fn() }, sessionManager: { getBranch: () => [] } };
}

describe("createTool", () => {
	it("has correct metadata", () => {
		const tool = createTool(stubPi(), "/agents");
		expect(tool.name).toBe("subagent");
		expect(tool.parameters).toBeDefined();
	});

	it("execute returns started message for run", async () => {
		const pi = stubPi();
		const tool = createTool(pi, `${import.meta.dirname}/../agents`);
		const result = await tool.execute("", { command: "runs" }, undefined, undefined, stubCtx());
		expect(result.content[0].text).toBeDefined();
	});

	it("handles unknown command", async () => {
		const tool = createTool(stubPi(), "/agents");
		const result = await tool.execute("", { command: "invalid" }, undefined, undefined, stubCtx());
		expect(result.isError).toBe(true);
	});
});
```

- [ ] **Step 2: 구현**

```typescript
import type { SubagentPi } from "./types.js";
import { SubagentParams } from "./types.js";
import { parseCommand } from "./cli.js";
import { loadAgentsFromDir, getAgent } from "./agents.js";
import { executeSingle, executeBatch, executeChain } from "./execute.js";
import { addRun, nextId, removeRun, listRuns } from "./store.js";
import { addToHistory, getRunHistory } from "./session.js";
import { buildCallText, buildResultText } from "./render.js";
import { syncWidget } from "./widget.js";
import { readdirSync, readFileSync, existsSync, writeFileSync, mkdirSync } from "fs";
import { tmpdir } from "os";
import { join } from "path";
import { spawn } from "child_process";
import { createInterface } from "readline";
import { parseLine } from "./parser.js";
import { collectOutput, getPiCommand, buildArgs } from "./runner.js";
import { withRetry } from "./retry.js";
import { extractMainContext } from "./context.js";
import { MAX_RETRIES, RETRY_BASE_MS } from "./constants.js";
import type { AgentConfig, RunResult } from "./types.js";

function makeRunner(agentsDir: string, pi: SubagentPi, ctx: { sessionManager: { getBranch: () => unknown[] } }, main: boolean): (agent: AgentConfig, task: string) => Promise<RunResult> {
	return async (agent, task) => {
		const id = nextId();
		const promptPath = join(tmpdir(), `pi-sub-${agent.name}-${id}.md`);
		let prompt = agent.systemPrompt;
		if (main) prompt += `\n\n[Main Context]\n${extractMainContext(ctx.sessionManager.getBranch() as Array<Record<string, unknown>>, 20)}`;
		writeFileSync(promptPath, prompt);
		const { cmd, base } = getPiCommand(process.execPath, process.argv[1], existsSync);
		const args = buildArgs({ base, model: agent.model, tools: agent.tools, systemPromptPath: promptPath, task });
		return withRetry(() => spawnAndCollect(cmd, args, id, agent.name), MAX_RETRIES, RETRY_BASE_MS);
	};
}

function spawnAndCollect(cmd: string, args: string[], id: number, agentName: string): Promise<RunResult> {
	return new Promise((resolve, reject) => {
		const proc = spawn(cmd, args, { stdio: ["ignore", "pipe", "pipe"] });
		const events: import("./parser.js").ParsedEvent[] = [];
		const rl = createInterface({ input: proc.stdout });
		rl.on("line", (line) => { const evt = parseLine(line); if (evt) events.push(evt); });
		proc.on("error", (err) => reject(err));
		proc.on("close", (code) => {
			const { output, usage, escalation } = collectOutput(events);
			if (code !== 0 && !output) { reject(new Error(`Process exited with code ${code}`)); return; }
			resolve({ id, agent: agentName, output, usage, escalation });
		});
	});
}

export function createTool(pi: SubagentPi, agentsDir: string) {
	const agents = existsSync(agentsDir) ? loadAgentsFromDir(agentsDir, readdirSync, readFileSync) : [];
	return {
		name: "subagent",
		label: "Subagent",
		description: "Run isolated subagent processes. Commands: run, batch, chain, continue, detail, runs, abort",
		parameters: SubagentParams,
		async execute(
			_id: string, params: { command: string }, signal: AbortSignal | undefined,
			_onUpdate: unknown, ctx: { hasUI: boolean; ui: { setWidget: Function }; sessionManager: { getBranch: () => unknown[] } },
		) {
			try { return dispatch(parseCommand(params.command), agents, pi, ctx, agentsDir); }
			catch (e) { return { content: [{ type: "text" as const, text: `Error: ${e instanceof Error ? e.message : e}` }], isError: true }; }
		},
	};
}

function dispatch(cmd: import("./types.js").Subcommand, agents: AgentConfig[], pi: SubagentPi, ctx: any, agentsDir: string) {
	if (cmd.type === "runs") return { content: [{ type: "text" as const, text: formatRunsList(listRuns(), getRunHistory()) }] };
	if (cmd.type === "detail") return { content: [{ type: "text" as const, text: formatDetail(cmd.id) }] };
	if (cmd.type === "run" || cmd.type === "continue") return dispatchRun(cmd, agents, pi, ctx);
	if (cmd.type === "batch") return dispatchBatch(cmd, agents, pi, ctx);
	if (cmd.type === "chain") return dispatchChain(cmd, agents, pi, ctx);
	return { content: [{ type: "text" as const, text: "Unknown command" }], isError: true };
}
```

참고: `dispatch*` 함수들은 서브프로세스를 비동기로 시작하고 즉시 "시작됨" 반환. 완료 시 `pi.sendMessage`로 followUp 전달. `tool.ts`가 99줄을 초과할 경우 dispatch 로직을 별도 파일 `dispatch.ts`로 분리한다.

- [ ] **Step 3: 테스트 통과 확인 + 커밋**

```bash
npx vitest run tests/tool.test.ts
git add src/tool.ts tests/tool.test.ts
git commit -m "subagent tool 팩토리"
```

---

## Task 17: index.ts

진입점. todo 확장과 동일한 패턴.

**Files:**
- Create: `src/index.ts`

- [ ] **Step 1: 구현**

```typescript
import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";
import { createTool } from "./tool.js";
import { restoreRuns, buildRunsEntry } from "./session.js";
import { syncWidget } from "./widget.js";
import { listRuns } from "./store.js";
import { dirname, join } from "path";
import { fileURLToPath } from "url";

export default function (pi: ExtensionAPI) {
	pi.on("session_start", async (_event, ctx) => {
		restoreRuns(ctx.sessionManager.getBranch());
	});
	pi.on("session_tree", async (_event, ctx) => {
		restoreRuns(ctx.sessionManager.getBranch());
	});
	pi.on("agent_end", async (_event, ctx) => {
		pi.appendEntry("subagent-runs", buildRunsEntry());
		syncWidget(ctx, listRuns());
	});
	pi.registerTool(createTool(pi, join(dirname(fileURLToPath(import.meta.url)), "..", "agents")));
}
```

- [ ] **Step 2: 빌드 확인**

```bash
cd 01_EXTENSIONS/subagent && npm run build
```

- [ ] **Step 3: 커밋**

```bash
git add src/index.ts
git commit -m "subagent index.ts 진입점"
```

---

## Task 18: Agent Definitions

**Files:**
- Create: `agents/scout.md`
- Create: `agents/worker.md`
- Create: `agents/reviewer.md`

- [ ] **Step 1: scout.md**

```markdown
---
name: scout
description: Fast codebase recon that returns compressed context
model: gpt-5.4-mini
thinking: low
tools: read, grep, find, ls, bash
---
You are a fast codebase scout. Your job is to quickly investigate and return structured findings.

Output format:
- Files found (with line ranges)
- Key code snippets
- Architecture notes
- "Start here" pointer for the next agent

Keep output concise. The agent receiving your output has NOT seen these files.
```

- [ ] **Step 2: worker.md**

```markdown
---
name: worker
description: General-purpose implementation agent
model: gpt-5.4
thinking: medium
---
You are a general-purpose implementation agent working in an isolated context.

Rules:
- Work autonomously on the given task
- If you are unsure about something critical, output [ESCALATION] followed by your question
- Report: files changed, key decisions, notes for the main agent
```

- [ ] **Step 3: reviewer.md**

```markdown
---
name: reviewer
description: Code review specialist
model: gpt-5.4
thinking: high
tools: read, grep, find, ls, bash
---
You are a senior code reviewer. You do NOT modify files.

Bash is restricted to read-only commands: git diff, git log, git show.

Output format:
- Critical issues (must fix)
- Warnings (should fix)
- Suggestions (nice to have)

Each item: file path, line number, description, suggested fix.
```

- [ ] **Step 4: 커밋**

```bash
git add agents/
git commit -m "subagent 에이전트 정의 (scout, worker, reviewer)"
```

---

## Task 19: Build, dist, README Sync

**Files:**
- Update: `01_EXTENSIONS/subagent/dist/index.js`
- Update: `README` (root)

- [ ] **Step 1: dist 빌드**

```bash
cd 01_EXTENSIONS/subagent && npm run build
```

- [ ] **Step 2: 전체 테스트**

```bash
cd 01_EXTENSIONS/subagent && npm test
```

Expected: ALL PASS, 100% coverage

- [ ] **Step 3: 루트 README 업데이트**

README의 EXTENSIONS 섹션에 추가:
```
    subagent … 서브에이전트 오케스트레이션 (비동기 실행, 세션 관리)
```

- [ ] **Step 4: 아키텍처 테스트 전체 실행**

```bash
cd 00_ARCHITECTURE/tests && go test ./... -v
```

- [ ] **Step 5: 커밋**

```bash
git add dist/ README
git commit -m "subagent dist 빌드 + README 동기화"
```
