{
  "pi": {
    "extensions": ["01_EXTENSIONS"],
    "skills": ["02_SKILLS"],
    "prompts": ["03_PROMPTS"],
    "themes": ["04_THEMES"]
  },
  "scripts": {
    "setup": "command -v brew >/dev/null || /bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\" && command -v mise >/dev/null || brew install mise && mise trust && mise install",
    "test": "cd 00_ARCHITECTURE/tests && go test -count=1 ./..."
  }
}
